{
  "name": "vagus-deployment-mcp",
  "version": "1.0.0",
  "description": "MCP Server for Vagus App Deployment Control",
  "main": "mcp-vagus-deployment.js",
  "scripts": {
    "start": "node mcp-vagus-deployment.js",
    "install-deps": "npm install @modelcontextprotocol/sdk pg"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
